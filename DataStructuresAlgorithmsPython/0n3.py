colors = ['green', 'blue', 'red', 'black', 'white']

def color_0n(colors):
    for c1 in colors:
        for c2 in colors:
            for c3 in colors:
                print(c1, c2, c3)

color_0n(colors)